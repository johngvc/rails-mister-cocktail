<h1>This is the new view of doses</h1>

<h1>Create your new dose for <%= @cocktail.name %></h1>
<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.input :ingredient %>
  <%= f.submit "Create!", class: "btn btn-primary" %>
<% end %>